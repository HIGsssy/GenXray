# Discord Bot credentials
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DISCORD_GUILD_ID=              # For guild-scoped command registration in dev

# Comma-separated list of channel snowflakes where /gen is allowed
ALLOWED_CHANNEL_IDS=

# ComfyUI backend
COMFY_BASE_URL=http://127.0.0.1:8188
# Milliseconds to wait for a single job before marking it failed (default 5 min)
COMFY_TIMEOUT_MS=300000

# Queue — must remain 1 for v1
QUEUE_CONCURRENCY=1

# SQLite database path
DB_PATH=./data/comfygen.db

# Logging: trace | debug | info | warn | error | fatal
LOG_LEVEL=info
